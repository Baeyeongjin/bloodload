# Bloodlord 규칙

## 영웅 스프라이트 방향
- **인게임 화면에서는 무조건 오른쪽을 본다.** 이게 사장님이 보는 기준.
- **소스 PNG는 왼쪽 보기로 저장한다.** 발렌티노 원본부터 전 자산의 규약이고 `Main.gd`의 `_hero.flip_h = true`가 뒤집는다. 소스를 오른쪽 보기로 두면 인게임에서 왼쪽을 본다(마왕 스킨에서 실제로 난 사고).
- **사장님께 보내는 확인판·GIF·미리보기는 반드시 좌우 반전(오른쪽 보기)해서 만든다.** 소스 방향 그대로 보내면 "왼쪽 본다"고 지적이 온다(2026-08-24 두 번).
- `install_motion.py` 자동 반전은 붉은 갑주 스킨에서 오판한다 — `--no-flip` + 눈으로 확인.
- PixelLab 생산 프롬프트: "STRICT SIDE VIEW, faces LEFT in every frame".

## "각각 UI 다르게" = 탭마다 전용 세트
사장님이 "각각 UI는 다 뽑아버리자"라고 하면 **탭마다 재질이 다른 전용 세트**(카드 틀·소탭 알약·버튼)를 뜻한다. 상점 세트를 소환·도전에 돌려썼다가 "분명 각각 다르게 해달라고 했을 텐데 다 같은 UI를 썼구나"라고 지적받았다(2026-08-13).

레퍼런스 방치형 RPG는 장소(대장간·점성소·던전·상점)마다 판 재질부터 달라서 "다른 가게에 들어온" 느낌을 준다. 틀 재사용은 그 감각을 죽인다. 새 화면·탭은 그 장소의 팔레트·모티프로 별도 생산.

## 설계 판단은 문서 4개에 있다
새 작업 전에 확인: `DESIGN`, `STATS`, `PLAN`, `BG_RECIPE` md.
곡선 숫자는 문서가 아니라 현재 기준선을 볼 것 — 200일 벽 굽힘 켜짐(k=0.65), x1.42/곡1.02.
