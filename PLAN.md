# Bloodlord — 방치형 기획

**흡혈귀 군주가 밤마다 사냥터를 넓혀 힘을 되찾고 빼앗긴 자기 성으로 돌아간다.**

arrow-rpg(액션 RPG)와 **별도 프로젝트**. 아트와 데이터 표만 재사용하고 게임 루프는 새로 짠다.
액션 RPG의 값은 조작·콤보·숙련인데 방치형에선 그게 전부 안 쓰이므로, 개조가 아니라 분리가 맞다.

주인공은 **발렌티노(뱀파이어 백작)** — arrow-rpg 로스터에서 뺐지만 자산(스프라이트 +
idle/walk/attack 애니)이 그대로 남아 있어 새로 만들 게 없다.

**자원은 골드가 아니라 피(血)다.** 흡혈로 성장하고, 관에서 잠든 동안 하수인이 피를
모아 온다. 리스킨이 아니라 방치형 루프가 주제와 맞물리는 지점이다 —
"자리를 비운 동안 골드가 쌓였다"보다 "관에서 자는 동안 하수인이 피를 모았다"가
방치라는 행위 자체에 이유를 준다.

---

## 1. 핵심 루프

```
자동 전투 → 피(血) → 성장 → 더 센 적 → 다음 스테이지 → (반복)
                                    ↕
                              자리 비움 → 오프라인 보상
```

**전투는 결정론적이다.** 회피도 조준도 없고 `DPS ÷ 적 체력`이 전부.
이건 게으름이 아니라 필요조건이다 — 오프라인 보상을 실제 시뮬레이션 없이
같은 공식으로 계산하려면 전투에 난수가 없어야 한다. 난수가 끼면
"껐다 켰더니 계산이 안 맞는다"가 반드시 나온다.

**플레이어가 하는 결정은 딱 하나: 피를 어디에 쓰는가.**
방치형에서 재미는 전투가 아니라 이 배분에서 나온다.

---

## 2. 콘텐츠 — 넣을 것과 안 넣을 것

방치형은 시스템을 많이 붙일 수 있어서 오히려 위험하다. 10개를 붙이면
각각 5분씩 관심받고 끝난다. **v1은 4개만** 하고 나머지는 이유를 적어 미룬다.

### v1에 넣는다

| 콘텐츠 | 왜 | 재사용 |
|---|---|---|
| **스테이지 진행** | 방치형의 뼈대. 숫자가 오르는 게 보상 | 몹 22종·막 5개 |
| **피 성장 (공격/속도/흡혈량)** | 유일한 결정 지점 | — |
| **장비 (수집·자동장착·강화)** | 방치의 "돌아올 이유". 자산이 이미 64종 있다 | 장비 아이콘 64종 |
| **도감** | 몹 22종 처치 기록. 장기 목표가 공짜로 생긴다 | 몹 스프라이트 22종 |
| **오프라인 보상** | 방치형의 정의 그 자체 | — |

### v2 이후로 미룬다

| 콘텐츠 | 왜 미루나 |
|---|---|
| 환생/전직 (프레스티지) | v1 진행이 얼마나 걸리는지 봐야 리셋 시점을 정할 수 있다. 먼저 만들면 감으로 잡게 된다 |
| 영웅 편성 (여러 명) | 캐릭터 9명 자산은 있지만, 편성은 "누구를 넣나"가 결정인데 v1엔 비교할 축이 없다 |
| 스킬 해금 | 자동 전투에 스킬을 넣으면 그냥 DPS 숫자다. 규칙이 바뀌는 스킬은 조작이 있어야 값이 산다 |
| 가챠 | 뽑을 게 충분히 쌓인 뒤에. 지금 넣으면 장비 64종을 다 보고 끝난다 |
| 일일 미션·출석 | 붙잡을 콘텐츠가 있어야 붙잡는 장치가 의미 있다 |
| 던전·레이드 | 본편 루프가 재미있는 게 먼저 |

**미루는 것도 결정이다.** v1을 돌려 보고 "심심하다"가 어디서 나오는지 보고 고른다.

---

## 3. 수치 설계

| 항목 | 값 | 이유 |
|---|---|---|
| 막 | 5개 (깨어난 무덤 → 화형의 언덕 → 서리 봉인지 → 핏빛 성소 → 빼앗긴 본성) | arrow-rpg 자산 그대로 |
| 막당 단계 | 10 (10번째는 보스) | 배경 5장으로 50단계를 굴린다 |
| 통과 조건 | 20킬 (보스는 1킬) | |
| 적 강화 | `(1 + 0.35n) × 1.045^n` | 순수 지수면 20단계에서 15배가 돼 대기표가 된다 |
| 피 획득 | `1 + 0.55n` | 적보다 느리게 → 후반에 방치가 필요해진다 |
| 업글 비용 | `10 × 1.18^lv` | |
| 오프라인 | 효율 50% · 8시간 상한 | 방치가 접속보다 이득이면 게임을 안 켠다 |
| 오프라인 진행 | **생존 판정으로 클리어 가능한 최고 단계까지** | 막힌 단계가 접속 후 성장 목표로 바로 보인다 |

---

## 4. 아트 규격 — 여기가 통일성의 전부

### 픽셀 밀도: 전부 2배

해상도는 **세로 576 × 896** (모바일 방치형 표준). 격자 36 × 56칸.
배경 생성 규격은 `assets/bg/BG_RECIPE.md` 가 단일 출처다.

| 자산 | 원본 | 화면 | 1 원본px |
|---|---|---|---|
| 배경 | **768 × 160** (가로로 김) | 1536 × 320 | 2 screen px |
| 캐릭터·몹 | 32 × 32 | 64 × 64 | 2 screen px |
| 아이템·아이콘 | 32 × 32 | 64 × 64 | 2 screen px |
| UI 패널 | 가변 | 2배 | 2 screen px |

**모든 자산이 같은 밀도**라 배경과 캐릭터의 도트 크기가 어긋나지 않는다.
화면 격자 단위는 16px = 원본 8px (`Grid.UNIT`).

배경을 288×512로 잡은 건 `create_image_pixen`의 한계(면적 ≤ 512×512, 변 ≤ 768,
변은 4의 배수) 안에서 2배 확대로 576×1024가 딱 떨어지기 때문이다.

**화면 구획** (16px 격자 기준 행 번호):

| 행 | 무엇 |
|---|---|
| 1–10 | 상단 상태창 (단계·피·초당 피해·진행바) |
| 11–36 | 전투 띠 (`BATTLE_Y = 540`) |
| 37–57 | 콘텐츠 창 — 성장/장비/도감이 이 자리를 나눠 쓴다 |
| 58–63 | 탭바 |

콘텐츠 창이 한 자리를 돌려 쓰는 이유: 세로 화면에서 창을 쌓으면 전투가 안 보인다.
"전투" 탭은 창을 전부 닫는 탭이다 — 그 자리에 넣을 다른 게 없다.

### 프롬프트 고정 틀

지난 세션에서 확인한 것: **PixelLab은 구체적인 물건은 잘 그리고 추상적인 효과는
전부 "공"으로 뭉갠다.** 그리고 부정문("no frame")은 안 먹히거나 반대로 받는다.
아래 틀은 그 실측에 맞춰 잡았다.

**공통 꼬리표 (모든 생성에 그대로 붙인다):**
```
flat muted colors, plain retro game pixel art
```

**UI / 아이콘** — `create_image_pixen`
```
description : small simple {물건}, {색}, flat muted colors, plain retro game pixel art
size        : 32 x 32 (아이콘) / 가변 (패널)
detail      : low detail
outline     : single color black outline
view        : side
```

**배경** — `create_image_pixen`
```
description : {장소} background scene, {분위기}, flat muted colors, plain retro game pixel art
size        : 640 x 360
detail      : low detail
outline     : lineless          ← 배경에 검은 외곽선이 들어가면 캐릭터가 안 떠 보인다
view        : side
no_background : false
```

**팔레트 통일**: 5막 전부 같은 시드 + 어두운 바탕 톤. 막 색은 설명에 넣는다
(무덤=차가운 회녹, 화형터=주황적, 봉인지=청백, 성소=핏빛 적, 본성=적자).

**한 번에 다 뽑지 않는다.** 배경은 막 1개(깨어난 무덤)만 먼저 뽑아 톤을 확정하고,
승인 후 나머지 4장. UI도 패널 1개 먼저. — 지난번 아이콘 작업에서 방향을 두 번
헛짚어 $0.05를 버린 게 이유다.

---

## 5. UI 목록 (생성 대상)

방치형 표준 구조: **상단 상태 · 중앙 전투 · 하단 탭바**

| 자산 | 크기(원본) | 개수 |
|---|---|---|
| 하단 탭바 배경 | 640 × 48 | 1 |
| 탭 아이콘 (전투·성장·장비·도감) | 32 × 32 | 4 |
| 패널 (성장·장비 창) | 320 × 240 | 1 (9-slice) |
| 버튼 (기본·눌림·비활성) | 96 × 32 | 3 |
| 진행바 (틀 + 채움) | 256 × 16 | 2 |
| 피·보석 아이콘 | 32 × 32 | 2 |
| 스탯 아이콘 (공격·속도·흡혈) | 32 × 32 | 3 |
| 장비 슬롯 틀 | 40 × 40 | 1 |
| 등급 테두리 (일반~전설) | 40 × 40 | 4 |
| **합계** | | **21장** |

배경 5장 + UI 21장 = **26 생성** (보유 1888개 — 여유 충분).

---

## 6. 진행 순서

1. ~~배경 1장(깨어난 무덤) 뽑아 톤 확정~~ ✔
2. ~~배경 4장 추가~~ ✔
3. ~~UI 패널 1개 뽑아 톤 확정~~ ✔
4. ~~UI 나머지~~ ✔ (9-slice로 21장 → 17장)
5. ~~코드: 배경 연결 · UI 교체 · 장비 시스템 · 탭 전환 · 도감~~ ✔
6. **돌려 보고 v2 콘텐츠 고르기** ← 지금 여기

---

## 7. 이미 만들어 둔 것 (돌아감)

```
Grid.gd       16px 격자 단일 출처 (u(3) 같은 유닛 단위로만 배치)
StageDefs.gd  5막 × 10단계 + 강화/피 곡선 (뱀파이어 서사)
FoeTiers.gd   몹 22종
Foe.gd        방치형 몹 (arrow-rpg Enemy 600줄 → 90줄)
Main.gd       자동 전투 + 진행 + 피 업글 + 오프라인 보상 + 탭 창 3개
GearDefs.gd   장비 굴림/등급/강화 (아이콘 64종 파일명에서 이름 조합)
Type.gd       폰트 단일 출처 (Fantasy Warrior, 11px 배수로만 쓴다)
Ui.gd         9-slice 패널·버튼·바 조립
tests/GearTest.gd  장비·도감 자체 점검 (godot --headless --script)
```

**장비 규칙**: 잡몹 4% / 보스 확정 드랍 → 더 센 것만 자동 장착, 약한 건 피로 분해.
인벤토리를 안 두는 이유는 방치형에서 정리를 시키면 방치가 아니게 되기 때문이다.

배경 자산이 없는 동안은 막별 단색 폴백으로 그린다 — 그림이 없어도 진행은 보인다.
