# 2026-09-09 이미지 작업 취소·원본 복원

후속 요청: 보스 특수패턴과 VFX 제작만 다시 승인됐으며, 사용자가 LibreSprite를 선택했다. 새 VFX는 `assets/anim/boss_vfx/`에 별도로 작성한다. 캐릭터·몬스터·배경 복원과 false 플래그는 유지한다. 자세한 범위는 `docs/BOSS_PATTERNS.md` 참조.

사용자 요청: **이미지들 그냥 옛날걸로 돌려라 전부**.

- Git HEAD bac48a6(2026-09-04)의 작업 전 원본 이미지로 복원했다.
- 변경된 PNG 556장을 원본 바이트로 복원했다. 캐릭터·몬스터·VFX·장비를 포함한다.
- 배경은 새 작업을 아직 적용하지 않아 원본 그대로다.
- 새 이미지 1,830장과 해당 import 메타데이터는 게임의 assets에서 분리했다.
  `build/qa/image-rollback-20260909/unused-new-images`에만 보관한다.
- Foe.pixel_motion_pilot은 false이며 새 동작·초상화·전용 VFX 표시 경로는 비활성화했다.
- 갑옷 재생성·12프레임 제작·17종 배경 생성 작업은 취소했다. QA 시안은 게임에서 사용하지 않는다.
- 사용자가 새로 요청하기 전까지 이전 제작 계획을 재개하거나 새 이미지를 다시 연결하지 않는다.

복원 전 바이트·원본 SHA256·분리 목록: `build/qa/image-rollback-20260909/`.
이미지 외의 저장 데이터와 게임 진행 상태는 복원 대상으로 삼지 않았다.

원본 자산 차이 0·신규 활성 이미지 0 확인. 임포트·게임 시작·전투 규칙·독립 PCK 검증 통과.
Bloodlord-Actions.cmd가 사용하는 build/Bloodlord-actions.pck도 원본 이미지로 재빌드했다.
