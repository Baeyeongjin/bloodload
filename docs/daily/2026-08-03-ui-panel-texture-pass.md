# 2026-08-03 공용 패널 내부 질감 보정

## 피드백

- `panel_clean.png`의 평평한 회색 내부가 기존 묘지·석재 UI와 어울리지 않음
- 모서리 안쪽 붉은 `ㄴ`자 장식이 잘린 표시처럼 보임

## 시도

- PixelLab `create_image_pixen`으로 미세 석재 결 공용 패널 생성
  - 시안: `assets/ui/panel_textured.png`
  - Job: `dd938908-3e19-4fd7-850d-5f3ad598752f`
- 큰 돌 외곽선이 생긴 1차 시안은 늘릴 때 과장될 수 있어 적용하지 않음
  - Job: `b046e84b-bb62-40d8-b526-35bc5f2ffb0e`
- 붉은 내부 모서리를 제거하고 둥근 금속 리벳 모서리로 교체한 시안을 검토

## 최종 결정

- 사용자 재검토 결과 새 사선 패턴이 기존 UI보다 어울리지 않아 적용 취소
- 공용 창은 최초 `assets/ui/panel.png`와 원래 색상으로 복귀
- 기존 `Ui.panel()` 9-slice로 상단 HUD와 콘텐츠 영역을 끝까지 채움
- 하단 탭 글씨를 각 칸의 `y=54~78` 중앙에 맞춰 붉은 선택선에서 위로 분리

## 확인

- Godot 576×896 성장·소환 화면에서 시안을 비교한 뒤 원복 결정
- 하단 전용 `tab_cell.png`는 변경하지 않음
