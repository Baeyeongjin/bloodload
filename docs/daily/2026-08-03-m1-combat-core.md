# M1 전투 코어 완료 (2026-08-03)

> 작업일: 2026-08-03

`docs/HANDOFF.md`의 M1 2-1~2-3을 구현하고 검증했다.

- 몹 22종의 `{key}_attack` 7프레임을 자기 공격 주기에 연결
- 몹/영웅 모두 4번째 프레임에 실제 타격
- 전열 `FRONT_X`를 300에서 235로 이동
- `fx_cleave`를 맞은 몹 위치에서 5프레임 재생
- 영웅 최대 체력·회복, 피격 hurt+흰 플래시, 사망·3초 부활 구현
- 사망 시 현재 단계 처치 수만 0으로 초기화
- 사망 연출: 상승+페이드와 `fx_death_blood` 7프레임
- 체력/체력회복 스탯을 실제 효과에 연결하고 `impl = true`로 전환
- 오프라인 진행을 로스터 평균 기반 결정론적 생존 판정으로 변경

검증:

- Godot import/메인 씬 로드 통과
- `BalanceTest OK`, `GearTest OK`
- 격리 저장본 전투 40초(사망·부활 구간 포함) 고속 시뮬레이션 오류 없음
- 576×896 8초 자동 캡처로 HP 표시·전열·타격 이펙트 확인

추가 환경 작업:

- 전역 npm에는 요청한 2.3.1보다 최신 `gopeak@2.3.8`이 설치돼 있었음
- Codex 전역 MCP에 `gopeak`를 `compact` 프로필로 등록하고 enabled 상태 확인
- 현재 작업에 도구를 주입하려면 Codex 앱 재시작 필요
- GoPeak의 editor/runtime 고급 기능은 프로젝트 애드온 설치·활성화가 별도 필요
