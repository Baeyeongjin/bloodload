# M2 정수 재화 분리

> 작업일: 2026-08-03

## 완료

- 저장 지갑에 `essence`를 추가하고 기존 저장은 기본값 0으로 읽는다.
- 보스 처치 시 `25 + (큰 단계 - 1) × 5` 정수를 지급한다.
- 현재 장비보다 약한 드랍은 `ceil(장비 전투력 × 2)` 정수로 자동 분해한다.
- 장비 강화 비용을 피에서 정수로 바꿨다.
- 장비 창에 정수 잔액과 강화 비용을 표시한다.

보석·인장·하수인 조각은 M2 범위에 넣지 않았다.

## 검증

- `BalanceTest.gd`, `GearTest.gd`, `CombatRulesTest.gd` 통과
- 메인 씬 600프레임 오류 없음
- 장비 창 실제 렌더에서 잔액·버튼·안내문 겹침 없음

다음 작업은 M3 공용 뽑기 코어와 장비·스킬 뽑기다.
